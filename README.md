# mcp-multi-server-react-agent
CLI AI agent built with LangGraph + LangChain MCP Adapters, connecting to multiple MCP servers (Context7 via HTTP, Met Museum via STDIO) with a GPT-5-powered ReAct agent and session memory.
